<%= bootstrap_form_for(category) do |form| %>

  <div>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.rich_text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
